{% if content_items.searchHits|length > 0 %}
    {% for item in content_items.searchHits %}
        {{ render_esi(controller('ez_content:viewAction', {
            locationId: item.valueObject.contentInfo.mainLocationId,
            viewType: view_type
        })) }}
    {% endfor %}
{% endif %}
